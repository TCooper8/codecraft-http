# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /user/:id           controllers.UserController.get(id: String)
GET     /music/:id           controllers.MusicController.get(id: String)

PUT     /user               controllers.UserController.add
PUT     /music              controllers.MusicController.add
